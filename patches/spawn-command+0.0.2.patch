diff --git a/node_modules/spawn-command/lib/spawn-command.js b/node_modules/spawn-command/lib/spawn-command.js
index 24d56ab..3caa771 100644
--- a/node_modules/spawn-command/lib/spawn-command.js
+++ b/node_modules/spawn-command/lib/spawn-command.js
@@ -6,7 +6,8 @@ module.exports = function (command, options) {
   if (process.platform === 'win32') {
     file = 'cmd.exe';
     args = ['/s', '/c', '"' + command + '"'];
-    options = util._extend({}, options);
+    // Replace deprecated util._extend with Object.assign for Node 22+
+    options = Object.assign({}, options);
     options.windowsVerbatimArguments = true;
   }
   else {
